<!doctype html>
<html data-wf-page="68e64cde361aeb3efeb34c85" data-wf-site="68e64cde361aeb3efeb34ca9" lang="{{ request.locale.iso_code }}">
<head>
  <meta charset="utf-8">
  <title>{{ page_title }}</title>
  <meta content="{{ page_description }}" name="description">
  <meta content="{{ page_title }}" property="og:title">
  <meta content="{{ page_description }}" property="og:description">
  <meta content="{{ page_title }}" property="twitter:title">
  <meta content="{{ page_description }}" property="twitter:description">
  <meta property="og:type" content="website">
  <meta content="summary_large_image" name="twitter:card">
  <meta content="width=device-width, initial-scale=1" name="viewport">
  <meta content="Webflow" name="generator">
  <link href="{{ 'normalize.css' | asset_url }}" rel="stylesheet" type="text/css">
  <link href="{{ 'webflow.css' | asset_url }}" rel="stylesheet" type="text/css">
  <link href="{{ 'verawang.webflow.css' | asset_url }}" rel="stylesheet" type="text/css">
  <script src="https://use.typekit.net/tfa1xuf.js" type="text/javascript"></script>
  <script type="text/javascript">try{Typekit.load();}catch(e){}</script>
  <script type="text/javascript">!function(o,c){var n=c.documentElement,t=" w-mod-";n.className+=t+"js",("ontouchstart"in o||o.DocumentTouch&&c instanceof DocumentTouch)&&(n.className+=t+"touch")}(window,document);</script>
  <link href="{{ 'favicon.png' | asset_url }}" rel="icon" type="image/png" sizes="32x32" media="(prefers-color-scheme: light)">
  <link href="{{ 'favicon-dark.png' | asset_url }}" rel="icon" type="image/png" sizes="32x32" media="(prefers-color-scheme: dark)">
  
            {% if settings.apple_touch_icon == blank %}
              <link href="{{ 'webclip.png' | asset_url }}" rel="apple-touch-icon">
            {% else %}
              <link rel="apple-touch-icon" type="image/png" href="{{ settings.apple_touch_icon | img_url: '32x32' }}">
            {% endif %}
        <!--  Google Tag Manager
<script>(function(w,d,s,l,i){w[l]=w[l]||[];w[l].push({'gtm.start':
new Date().getTime(),event:'gtm.js'});var f=d.getElementsByTagName(s)[0],
j=d.createElement(s),dl=l!='dataLayer'?'&l='+l:'';j.async=true;j.src=
'https://www.googletagmanager.com/gtm.js?id='+i+dl;f.parentNode.insertBefore(j,f);
})(window,document,'script','dataLayer','GTM-58VLFZFL');</script>
End Google Tag Manager  -->
  <script>
(function () {
  var origPush = window.history.pushState.bind(window.history);
  window.history.pushState = function (state, title, url) {
    if (typeof url === 'string' && /[?&]variant=/.test(url)) {
      return window.history.replaceState(state, title, url);
    }
    return origPush(state, title, url);
  };
})();
</script>
  <script src="
https://cdn.jsdelivr.net/npm/keen-slider@6.8.6/keen-slider.min.js
"></script>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.css">
  <script src="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.js"></script>
  <script src="https://cdn.goodcraft.io/api/loader/a862721e-575e-4e0e-b1b0-dfaefc38d694"></script>
  <!--  [Attributes by Finsweet] Modal  -->
  <script async="" src="https://cdn.jsdelivr.net/npm/@finsweet/attributes-modal@1/modal.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/gsap@3.14.1/dist/gsap.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/gsap@3.14.1/dist/ScrollTrigger.min.js"></script>
  <script defer="" auto="" min="" src="https://cdn.jsdelivr.net/gh/liquify-pro/core@latest/script.js"></script>
  <!--  Alpine Focus Plugin  -->
  <script defer="" src="https://cdn.jsdelivr.net/npm/@alpinejs/focus@3.x.x/dist/cdn.min.js"></script>
  <!--  Alpine JS  -->
  <script defer="" src="{{ 'alpinejs3.js' | asset_url }}"></script>
  <!--  Liquify Pro Debugger  -->
  <!-- <script defer auto min src="https://cdn.jsdelivr.net/gh/liquify-pro/core@latest/script.js"></script> -->
  <link href="{{ canonical_url }}" rel="canonical">
  {% if page_image %}
  <meta property="og:image" content="http:{{ page_image | image_url }}">
  <meta property="og:image:secure_url" content="https:{{ page_image | image_url }}">
  <meta property="og:image:width" content="{{ page_image.width }}">
  <meta property="og:image:height" content="{{ page_image.height }}">
{% endif %}
  {% case template.name %}
  {% when 'product' %}
<script type="application/ld+json">
  {{ product | structured_data }}
</script>
{% when 'article' %}
<script type="application/ld+json">
  {{ article | structured_data }}
</script>
{% when 'collection' %}
<script type="application/ld+json">
                  {
                     "@context":"http:\/\/schema.org",
                     "@type":"WebPage",
                     "url":"{{ shop.url }}",
                     "mainEntity":{"@type":"offerCatalog",
                     "name": "{{ collection.title }}",
                     "url": "{{ collection.url }}",
                     "numberOfItems":{{ collection.products.size }}}
              }
</script>
{% endcase %}
{% assign min_price = null %}
{% assign max_price = 0 %}
{% for product in collections.all-products.products %}
  {% for variant in product.variants %}
    {% assign price = variant.price | money_without_currency | replace: ',', '.' | plus: 0 %}
    {% if min_price == null or price < min_price %}
      {% assign min_price = price %}
    {% endif %}
    {% if price > max_price %}
      {% assign max_price = price %}
    {% endif %}
  {% endfor %}
{% endfor %}
    <script type="application/ld+json">
      {
        "@context" : "https://schema.org",
        "@type" : "LocalBusiness",
        "mainEntityOfPage" : {
          "@type" : "WebPage",
          "@id" : "{{ shop.url }}"
        },
        "name" : "{{ shop.name }}",
        "telephone" : "{{ shop.phone }}",
        "email" : "{{ shop.email }}",
        "priceRange" : "{{ min_price | money }} - {{ max_price | money }}",
        "address" : {
          "@type" : "PostalAddress",
          "streetAddress" : "{{ shop.address.address1 }}",
          "addressLocality" : "{{ shop.address.city }}",
          "addressRegion" : "{{ shop.address.province }}",
          "addressCountry" : "{{ shop.address.country_code }}",
          "postalCode" : "{{ shop.address.zip }}"
        },
        "url" : "{{ shop.url }}"
      }
    </script>
  <style>
  /* AlpineJS can cause a brief &ldquo;blip&rdquo; where uninitialized templates flash before it loads. x-cloak prevents this by hiding elements until Alpine is ready. Learn more: https://alpinejs.dev/directives/cloak */
  [x-cloak] { display: none !important; }
  /* The li-cloak tag is removed during conversion. It&rsquo;s useful for hiding elements in the Webflow Designer to make editing easier. Learn more: https://www.liquify.pro/docu/getting-started#li-cloak */  
  [li-cloak_disabled] { display: none !important; }
  /* Setup Font and Background Color if Text is selected */
  ::selection {
    color: #FFFFFF;
    background: #000000;
  }
  ::-moz-selection {
    color: #FFFFFF;
    background: #000000;
  }
</style>
  <!--  
IMPORTANT: If you want to make changes to the head or body code on this page, 
please remove the `li-layout` tag from the body element. 
This will create a separate layout file for this page, allowing you to apply custom modifications.
Learn more: https://www.liquify.pro/docu/getting-started#template-structure
 -->
  <script src="https://cdn.prod.website-files.com/68e64cde361aeb3efeb34ca9%2F689e5ba67671442434f3ca35%2F69fc96205d8bf15349778e97%2Fsize_picker_state_css-1.0.0.js" type="text/javascript"></script>
  <script src="https://cdn.prod.website-files.com/68e64cde361aeb3efeb34ca9%2F689e5ba67671442434f3ca35%2F69fc972b4468d8fc64e6baa8%2Fsize_picker_state_css-1.1.0.js" type="text/javascript"></script>
<script src="{{ 'li_helper.js' | asset_url }}" type="text/javascript"></script>{{ content_for_header }}</head>
<body li-page="account">
  <div class="page-wrapper">
    {% render 'global_styles' %}
    {% sections 'header_group' %}
    {{ content_for_layout }}
    {% sections 'footer_group' %}
  </div>
  <script src="https://d3e54v103j8qbb.cloudfront.net/js/jquery-3.5.1.min.dc5e7f18c8.js?site=68e64cde361aeb3efeb34ca9" type="text/javascript" integrity="sha256-9/aliU8dGd2tb6OSsuzixeV4y/faTqgFtohetphbbj0=" crossorigin="anonymous"></script>
  <script src="{{ 'webflow.js' | asset_url }}" type="text/javascript"></script><!--  Google Tag Manager (noscript) 
<noscript><iframe src="https://www.googletagmanager.com/ns.html?id=GTM-58VLFZFL"
height="0" width="0" style="display:none;visibility:hidden"></iframe></noscript>
End Google Tag Manager (noscript)  -->
  <script src="{{ 'section-rendering.js' | asset_url }}"></script>
  {% render 'search_javascript' %}
  <script>
(function () {
  function $ov() { return document.querySelector('[data-cart-overlay]'); }
  function $pn() { return document.querySelector('[data-cart-panel]'); }
  function $bd() { return document.querySelector('[data-cart-close].mini-cart-v2_backdrop'); } // &larr; new
  function openCart() {
    var ov = $ov(), pn = $pn(), bd = $bd();
    if (ov) ov.classList.add('is-open');
    if (pn) pn.classList.add('is-open');
    if (bd) bd.classList.add('is-open');                                                       // &larr; new
    document.body.setAttribute('data-cart-open', 'true');
    window.dispatchEvent(new CustomEvent('cartupdated'));
  }
  function closeCart() {
    var ov = $ov(), pn = $pn(), bd = $bd();
    if (ov) ov.classList.remove('is-open');
    if (pn) pn.classList.remove('is-open');
    if (bd) bd.classList.remove('is-open');                                                    // &larr; new
    document.body.removeAttribute('data-cart-open');
  }
  document.addEventListener('click', function (e) {
    if (e.target.closest('[data-cart-toggle]')) { e.preventDefault(); openCart(); return; }
    if (!document.body.hasAttribute('data-cart-open')) return;
    if (e.target.closest('[data-cart-close]') || e.target.closest('.nav_dropdown-close-button')) {
      e.preventDefault();
      closeCart();
    }
  });
  document.addEventListener('keydown', function (e) {
    if (e.key === 'Escape') closeCart();
  });
  window.addEventListener('toggleminicart', openCart);
})();
</script>
  <script>
(function () {
  'use strict';
  if (window._wlv4) return;
  window._wlv4 = true;
  var TRANS = 'transform 0.35s cubic-bezier(0.32,0.72,0,1)';
  /* Fix Goodcraft's portal-injected <style> tag (sits inside a <div>, loads after theme CSS) */
  function patchGoodcraftPortalCSS() {
    document.querySelectorAll('style').forEach(function (styleEl) {
      if (!styleEl.parentElement || styleEl.parentElement.tagName !== 'DIV') return;
      try {
        var rules = Array.from(styleEl.sheet.cssRules || []);
        rules.forEach(function (r) {
          /* Fix gap:0 !important on item card */
          if (r.selectorText === '.nm-wishlist-item-card' &&
              r.style.getPropertyPriority('gap') === 'important') {
            r.style.setProperty('gap', '12px', 'important');
          }
          /* Fix border-bottom on summery inside mobile media query */
          if (r.cssRules) {
            Array.from(r.cssRules).forEach(function (inner) {
              if (inner.selectorText === '.nm-wishlist-summery') {
                inner.style.removeProperty('border-bottom');
              }
            });
          }
        });
      } catch (e) {}
    });
  }
  /* On mobile: reposition wrapper to anchor at bottom, remove Goodcraft's height:100% on body */
  function positionWrapperForMobile(w) {
    w.style.setProperty('position', 'absolute', 'important');
    w.style.setProperty('bottom', '0', 'important');
    w.style.setProperty('left', '0', 'important');
    w.style.setProperty('right', '0', 'important');
    w.style.setProperty('top', 'auto', 'important');
    w.style.setProperty('width', '100%', 'important');
    w.style.setProperty('max-width', '100%', 'important');
    w.style.setProperty('height', 'auto', 'important');
    var drawer = w.querySelector('.nm-wishlist-drawer');
    if (drawer) {
      Array.from(drawer.children).forEach(function (child) {
        if (child.style && child.style.height === '100%') {
          child.style.removeProperty('height');
        }
      });
    }
  }
  /* Slide animations &mdash; use inline style.setProperty with 'important'
     so we beat Goodcraft's sheet-level transform:none !important */
  function slideIn(w) {
    w.style.setProperty('transition', TRANS, 'important');
    requestAnimationFrame(function () {
      requestAnimationFrame(function () {
        w.style.setProperty('transform', 'translateY(0%)', 'important');
      });
    });
  }
  function slideOut(w) {
    w.style.setProperty('transition', TRANS, 'important');
    requestAnimationFrame(function () {
      requestAnimationFrame(function () {
        w.style.setProperty('transform', 'translateY(110%)', 'important');
      });
    });
  }
  function snapToBottom(w) {
    w.style.setProperty('transition', 'none', 'important');
    w.style.setProperty('transform', 'translateY(110%)', 'important');
  }
  /* Watch .nm-modal-container opacity to drive the slide */
  function attachContainerWatcher() {
    var container = document.querySelector('.nm-modal-container');
    if (!container || container._wlWatched) return;
    container._wlWatched = true;
    function isMobile() { return window.innerWidth <= 767; }
    var w = container.firstElementChild;
    if (w && isMobile()) {
      positionWrapperForMobile(w);
      snapToBottom(w);
    }
    new MutationObserver(function () {
      if (!isMobile()) return;
      var w2 = container.firstElementChild;
      if (!w2) return;
      var st = container.getAttribute('style') || '';
      if (st.indexOf('opacity: 1') !== -1 || st.indexOf('opacity:1') !== -1) {
        positionWrapperForMobile(w2);
        slideIn(w2);
      } else if (st.indexOf('opacity: 0') !== -1 || st.indexOf('opacity:0') !== -1) {
        slideOut(w2);
      }
    }).observe(container, { attributes: true, attributeFilter: ['style'] });
  }
  /* Inject text REMOVE button into each card */
  function addRemoveButtons() {
    document.querySelectorAll('.nm-wishlist-item-card').forEach(function (card) {
      if (card.querySelector('.nm-wl-remove-text-btn')) return;
      var titleDiv = card.querySelector('.nm-wishlist-item-title');
      var iconBtn  = card.querySelector('.nm-wishlist-item-remove-button');
      if (!titleDiv || !iconBtn) return;
      var textBtn = document.createElement('button');
      textBtn.className   = 'nm-wl-remove-text-btn';
      textBtn.textContent = 'REMOVE';
      textBtn.type        = 'button';
      textBtn.addEventListener('click', function (e) {
        e.preventDefault();
        e.stopPropagation();
        iconBtn.click();
      });
      titleDiv.appendChild(textBtn);
    });
  }
  /* Upsize Shopify CDN images */
  function upsizeImages() {
    document.querySelectorAll('.nm-wishlist-item-image').forEach(function (img) {
      if (img.src && img.src.indexOf('cdn.shopify.com') !== -1 &&
          img.src.indexOf('width=600') === -1) {
        img.src = img.src.replace(/width=\d+/, 'width=600');
      }
    });
  }
  /* Keep cart count in sync after Add to Cart */
  if (!window._wlFetchPatched) {
    window._wlFetchPatched = true;
    var _origFetch = window.fetch;
    window.fetch = function () {
      var args = Array.prototype.slice.call(arguments);
      var url  = typeof args[0] === 'string' ? args[0] : (args[0] && args[0].url) || '';
      var p    = _origFetch.apply(this, args);
      if (url.indexOf('/cart/add') !== -1) {
        p.then(function () {
          fetch('/cart.js').then(function (r) { return r.json(); }).then(function (cart) {
            document.dispatchEvent(new CustomEvent('carttotalitems', {
              detail: { itemCount: cart.item_count }
            }));
          });
        }).catch(function () {});
      }
      return p;
    };
  }
  /* Main &mdash; runs on every DOM change (Goodcraft renders lazily) */
  function run() {
    patchGoodcraftPortalCSS();
    upsizeImages();
    addRemoveButtons();
    attachContainerWatcher();
  }
  new MutationObserver(run).observe(document.body, { childList: true, subtree: true });
  run();
})();
</script>
</body>
</html>